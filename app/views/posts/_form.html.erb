<% if current_user %>
  <%= form_for :post, url: posts_path do |f| %>
    <%= f.text_field :title %>
    <%= f.text_area :body %>
    <%= f.hidden_field :creator_id, value: current_user.id %>
  <% end %>
<% end %>